numbers = [1, 10, 5, 16, 4, 9, 10]
letters = ['a', 'g', 's', 'b', 'y', 'a', 's']

# value = max(numbers)     # min ve max metodları listedeki minimum ve maksimum değerleri verir
# value = min(letters)
# value = max(letters)
# value = numbers[3:6]

# numbers[4] = 40

# numbers.append(49)       # . append metodu listenin en sonuna bir eleman ekler.

# numbers.insert(3, 78)     # .insert metodu listenin girilen index numarasına eleman ekler, diğer elemanların index değeri bir artar.
# numbers.insert(-1, 52)

# numbers.pop()           # .pop metodu listenin en sonundaki elemanı siler
# numbers.pop(0)          # listenin girilen index numaralı elemanını siler

# numbers.remove(1)         # .remove metodu verilen değeri liste elemanları içerisinde arar ve siler, bulamazsa hata verir.

# numbers.sort()          # .sort metodu değerleri küçükten büyüğe veya alfabetik olarak sıralar.
# letters.sort()

# numbers.reverse()         # .reverse metodu elemanların index değerlerini ters çevirir ve listeyi ters yazar.

# numbers.clear()             # .clear metodu liste elemanlarını temizler

# print(len(numbers))
# print(len(letters))
# print(value)

# print(numbers.count(10))       # .count metodu listede belirtilen değişkenden kaç adet olduğunu sorgular.
# print(letters.count('a'))

print(numbers)
print(letters)